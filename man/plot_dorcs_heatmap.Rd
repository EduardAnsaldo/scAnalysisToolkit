% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmaps.r
\name{plot_dorcs_heatmap}
\alias{plot_dorcs_heatmap}
\title{Plot a heatmap of DORC accessibility across samples}
\usage{
plot_dorcs_heatmap(
  dorcs_to_plot,
  seurat,
  title,
  color_palette = grDevices::hcl.colors(n = 20, "RdBu", rev = TRUE),
  grouping_var = "Samples"
)
}
\arguments{
\item{dorcs_to_plot}{Character vector of DORC names to include in the heatmap.
The order of this vector determines the row order in the resulting plot.}

\item{seurat}{A Seurat object containing a \code{DORCs} assay. The assay should
have a populated \code{data} slot (i.e. normalized accessibility values).}

\item{title}{Character string used as the plot title. Typically the
name of the pathway or gene set the DORCs belong to.}

\item{color_palette}{Character vector of colors used for the fill gradient.
Defaults to a reversed RdBu palette with 20 colors, suitable for
diverging z-scored data.}

\item{grouping_var}{Character string giving the name of the metadata column
in the Seurat object to aggregate accessibility by. Defaults to \code{'Samples'}.}
}
\value{
A \code{ggplot} object showing a heatmap with DORCs on the y-axis,
samples (or other groups) on the x-axis, and tiles colored by z-scored
accessibility. The color scale is symmetric around zero.
}
\description{
Creates a heatmap visualizing z-scored DORC (Domains of Regulatory Chromatin)
accessibility across samples or other grouping variables. Accessibility values
are aggregated within groups and then z-scored across groups for each DORC,
allowing relative patterns of accessibility to be compared.
}
\details{
Accessibility values are aggregated within each group using
\code{Seurat::AggregateExpression()} with \code{return.seurat = TRUE}, then z-scored
across groups within each DORC using \code{scale()}. The color scale limits are
set symmetrically based on the maximum absolute z-score so that zero maps
to the midpoint of the palette.

Note that depending on how the \code{DORCs} assay was constructed, you may need
to use the \code{counts} slot instead of \code{data} if no normalization has been
applied.
}
\examples{
\dontrun{
dorcs <- c("GENE1", "GENE2", "GENE3")
plot_dorcs_heatmap(
    dorcs_to_plot = dorcs,
    seurat = seurat_obj,
    title = "Inflammatory Response",
    grouping_var = "Samples"
)
}

}
