% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_annotation.r
\name{annotate_seurat_with_LLM}
\alias{annotate_seurat_with_LLM}
\title{Annotate Seurat Clusters with an LLM (Claude)}
\usage{
annotate_seurat_with_LLM(
  seurat,
  tissue,
  design = "Single-cell RNA-seq; experimental design unspecified.",
  cluster_col = "seurat_clusters",
  label_col = "llm_celltype",
  model = "claude-opus-4-8",
  markers = NULL,
  n_markers = 25,
  assay = NULL,
  use_pubmed = TRUE,
  pubmed_n = 10,
  pubmed_email = NULL,
  max_tokens = 1024,
  temperature = 0,
  api_key = NULL,
  keyring_service = "anthropic",
  cache_dir = file.path(tempdir(), "llm_pubmed_cache"),
  dry_run = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{seurat}{Seurat object with cluster assignments.}

\item{tissue}{Character; tissue / sample description used as context, e.g.
"human peripheral blood mononuclear cells (PBMC)".}

\item{design}{Character; experimental-design context, e.g.
"healthy donor, 10x Genomics 3' scRNA-seq". Default is a generic placeholder.}

\item{cluster_col}{Character; metadata column holding cluster ids.
Default 'seurat_clusters'.}

\item{label_col}{Character; metadata column to write predicted labels into.
A companion \verb{<label_col>_confidence} column is also added. Default 'llm_celltype'.}

\item{model}{Character; Anthropic model id. Default 'claude-opus-4-8'.}

\item{markers}{Optional data frame of precomputed markers (a
\code{\link[Seurat:FindAllMarkers]{Seurat::FindAllMarkers()}} result, or any data frame with columns 'cluster',
'gene', and 'avg_log2FC'); skips marker computation. Default NULL.}

\item{n_markers}{Integer; number of top markers per cluster sent to the model.
Default 25.}

\item{assay}{Character; assay used for marker computation. Default NULL
(uses \code{\link[Seurat:reexports]{Seurat::DefaultAssay()}}).}

\item{use_pubmed}{Logical; retrieve PubMed abstracts for grounding. Default TRUE.}

\item{pubmed_n}{Integer; abstracts to retrieve per cluster. Default 10.}

\item{pubmed_email}{Character; contact email for NCBI E-utilities etiquette.
Default NULL.}

\item{max_tokens}{Integer; max output tokens per call. Default 1024.}

\item{temperature}{Numeric; sampling temperature. Default 0.}

\item{api_key}{Character; Anthropic API key. Default NULL, in which case the
key is taken from the system keyring (\code{keyring_service}) or the
\code{ANTHROPIC_API_KEY} environment variable.}

\item{keyring_service}{Character; keyring service name under which the API key
is stored (see \code{keyring::key_set()}). Default 'anthropic'.}

\item{cache_dir}{Character; directory for the PubMed abstract cache.
Default a session temp directory.}

\item{dry_run}{Logical; if TRUE, assemble prompts only (no API call) and store
them in \code{seurat@misc$llm_annotation_prompts}. Default FALSE.}

\item{verbose}{Logical; print progress messages. Default TRUE.}
}
\value{
The Seurat object with added metadata columns \code{label_col} and
\verb{<label_col>_confidence}, and the per-cluster results tibble stored in
\code{seurat@misc$llm_annotation} (columns: cluster, n_cells, top_markers,
cell_type, confidence, rationale, n_refs, input_tokens, output_tokens, raw,
error). With \code{dry_run = TRUE}, metadata is unchanged and the assembled
prompts are stored in \code{seurat@misc$llm_annotation_prompts} instead.
}
\description{
Context-aware, marker-based cell type annotation of a clustered Seurat object
using the Anthropic API. For each cluster the top marker genes (computed with
\code{\link[Seurat:FindAllMarkers]{Seurat::FindAllMarkers()}} or supplied) are sent to Claude together with the
tissue and experimental-design context and, optionally, abstracts retrieved
from PubMed, and the model returns the most specific defensible cell type. The
per-cluster label is written back into the object metadata (broadcast to every
cell in the cluster).
}
\details{
Requires an Anthropic API key and the \code{httr2} and \code{jsonlite} packages. The key
is resolved (in priority order) from the \code{api_key} argument, the system keyring
(recommended — store it once with \code{keyring::key_set("anthropic")}), then the
\code{ANTHROPIC_API_KEY} environment variable. Use \code{dry_run = TRUE} to assemble and
inspect the prompts without calling the API (no key needed).
}
\examples{
\dontrun{
# One-time: store the key securely in the OS keyring (prompts for the value).
keyring::key_set("anthropic")

seurat <- annotate_seurat_with_LLM(
    seurat,
    tissue = "human peripheral blood mononuclear cells (PBMC)",
    design = "healthy donor, 10x Genomics 3' scRNA-seq"
)
seurat@misc$llm_annotation        # per-cluster table

# Preview prompts without calling the API:
seurat <- annotate_seurat_with_LLM(seurat, tissue = "human PBMC", dry_run = TRUE)
seurat@misc$llm_annotation_prompts
}

}
