% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.r
\name{create_tracks_patchwork2}
\alias{create_tracks_patchwork2}
\title{Create a patchwork of genomic tracks for multiple regions/genes}
\usage{
create_tracks_patchwork2(
  seurat,
  regions,
  theme = NULL,
  nrow = NULL,
  ncol = NULL,
  title = NULL,
  subtitle = NULL,
  title_theme = NULL,
  assay_atac = "ATAC",
  extend_upstream = 20000,
  extend_downstream = 20000,
  annotation_font_size = 1.5,
  add_links = FALSE,
  gene_symbol = FALSE,
  expression_features = NULL,
  assay = "SCT",
  heights = c(10, 1, 2, 3),
  widths = c(10, 1),
  add_track_titles = FALSE,
  track_title_suffix = NULL,
  track_titles = NULL,
  track_title_theme = NULL
)
}
\arguments{
\item{seurat}{A Seurat object with ATAC data}

\item{regions}{Character vector of regions or gene names to plot.
Regions can be given as coordinate strings ("chr1-1000-2000") or as
gene symbols; gene symbols are resolved to coordinates using the
annotation stored in the object's ATAC assay.}

\item{theme}{A ggplot2 theme to apply to all plots}

\item{nrow}{Number of rows in the patchwork grid}

\item{ncol}{Number of columns in the patchwork grid}

\item{title}{Optional shared title for the full patchwork}

\item{subtitle}{Optional shared subtitle}

\item{title_theme}{Optional theme element for the title}

\item{assay_atac}{Name of the ATAC/peaks assay (default "ATAC")}

\item{extend_upstream}{Bases to extend upstream (default 20000)}

\item{extend_downstream}{Bases to extend downstream (default 20000)}

\item{add_links}{Logical, whether to add link plots (default FALSE)}

\item{gene_symbol}{Logical, whether to add expression plots (default FALSE)}

\item{expression_features}{Optional vector of features for the expression
plots, parallel to \code{regions}. If NULL, the region input itself is used.}

\item{assay}{Assay to use for expression plot (default "SCT")}

\item{heights}{Numeric vector of relative heights for tracks}

\item{widths}{Numeric vector of relative widths (track vs expression)}

\item{add_track_titles}{Logical; if TRUE, add a per-region title above each
track stack. The title is built from the gene symbol (the value passed in
\code{regions}) combined with \code{track_title_suffix}. Default FALSE.}

\item{track_title_suffix}{Optional string or vector to append after each
gene symbol in the per-track title. Accepts:
\itemize{
\item A single string: applied to all regions
\item A named character vector: looked up by region/gene name
\item An unnamed character vector: applied positionally to \code{regions}
}
Default NULL (gene symbol only).}

\item{track_titles}{Optional character vector of explicit titles to use
verbatim, length must equal \code{length(regions)}. Overrides the
\code{track_title_suffix} logic. Useful when passing coordinate strings as
regions. Default NULL.}

\item{track_title_theme}{Optional theme for per-track titles. Default is
centered, italic, size 12.}
}
\value{
A patchwork object
}
\description{
Create a patchwork of genomic tracks for multiple regions/genes
}
