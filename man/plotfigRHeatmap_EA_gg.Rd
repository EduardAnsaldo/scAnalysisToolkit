% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmaps.r
\name{plotfigRHeatmap_EA_gg}
\alias{plotfigRHeatmap_EA_gg}
\title{Plot a figR regulatory score heatmap (ggplot2 implementation)}
\usage{
plotfigRHeatmap_EA_gg(
  figR.d,
  score.cut = 1,
  DORCs = NULL,
  TFs = NULL,
  DORC_groups = NULL,
  cluster_DORCs = TRUE,
  cluster_TFs = TRUE,
  color_palette = grDevices::hcl.colors(n = 20, "RdBu", rev = TRUE),
  limits = c(-2, 2),
  plot_title = NULL,
  x_title = "TFs",
  y_title = "DORCs",
  highlight_TFs = NULL,
  highlight_label_color = "red",
  highlight_box_color = "red",
  highlight_box = TRUE,
  highlight_box_linewidth = 1,
  group_tag = "DORC groups"
)
}
\arguments{
\item{figR.d}{A data.frame of figR results containing at least the columns
\code{DORC}, \code{Motif}, and \code{Score}.}

\item{score.cut}{Numeric absolute score threshold. Applied only to the
axis (or axes) that the user did not specify. Ignored when both \code{DORCs}
and \code{TFs} are supplied.}

\item{DORCs}{Optional character vector of DORC names to restrict the plot to.}

\item{TFs}{Optional character vector of TF (Motif) names to restrict the
plot to.}

\item{DORC_groups}{Optional vector the same length as \code{DORCs} giving a
group label for each DORC. When supplied, the heatmap is faceted by group
along rows. Requires \code{DORCs} to be non-NULL.}

\item{cluster_DORCs}{Logical; whether to cluster DORCs (rows). Only takes
effect when \code{DORCs} is non-NULL. Forced to \code{FALSE} when \code{DORC_groups} is
supplied. Defaults to \code{TRUE}.}

\item{cluster_TFs}{Logical; whether to cluster TFs (columns). Only takes
effect when \code{TFs} is non-NULL. Defaults to \code{TRUE}.}

\item{color_palette}{Character vector of colors used for the fill gradient.
Defaults to a reversed RdBu palette suitable for diverging scores.}

\item{limits}{Numeric vector of length 2 giving the fill scale limits.
Defaults to \code{c(-2, 2)} to match the original implementation.}

\item{plot_title}{Optional plot title. Defaults to \code{NULL} (no title).}

\item{x_title}{Character string for the x-axis title. Defaults to \code{"TFs"}.}

\item{y_title}{Character string for the y-axis title. Defaults to \code{"DORCs"}.}

\item{highlight_TFs}{Optional character vector of TF (Motif) names to
highlight. Highlighted TFs receive bold, colored x-axis labels and, if
\code{highlight_box = TRUE}, a rectangle drawn around their column.}

\item{highlight_label_color}{Color used for highlighted TF axis labels.
Defaults to \code{"red"}.}

\item{highlight_box_color}{Color used for the rectangles drawn around
highlighted TF columns. Defaults to \code{"red"}.}

\item{highlight_box}{Logical; whether to draw a rectangle around the column
of each highlighted TF. Defaults to \code{TRUE}. Has no effect when
\code{highlight_TFs} is \code{NULL}.}

\item{highlight_box_linewidth}{Numeric line width for the highlight
rectangles. Defaults to \code{1}.}

\item{group_tag}{Character string used as the plot tag when \code{DORC_groups}
is supplied. Sits between the panel and the legend, rotated to read
vertically. Defaults to \code{"DORC groups"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualizes TF-DORC regulatory scores as a heatmap, with DORCs on rows and
TFs on columns. Both axes are ordered by hierarchical clustering using
Pearson correlation distance by default, mirroring the default behavior of
the \code{ComplexHeatmap}-based implementation.
}
\details{
Filtering behavior:
\itemize{
\item When both \code{DORCs} and \code{TFs} are specified, \code{score.cut} is ignored
and the heatmap shows all pairs from the user-supplied lists.
\item When only \code{DORCs} is specified, all supplied DORCs are kept (no
score filtering on rows), and TFs are filtered to those passing
\code{score.cut} for at least one of the supplied DORCs.
\item When only \code{TFs} is specified, all supplied TFs are kept (no score
filtering on columns), and DORCs are filtered to those passing
\code{score.cut} for at least one of the supplied TFs.
\item When neither is specified, both axes are filtered by \code{score.cut}.
}

Clustering can be disabled per-axis via \code{cluster_DORCs} and \code{cluster_TFs}.
When clustering is disabled and the corresponding axis was user-supplied,
the user's order is used but reversed (so the first element appears at the
top of the y-axis or the left of the x-axis, matching how the input is
typically read).

DORCs may optionally be split into groups via \code{DORC_groups}, in which case
the heatmap is faceted by group along the y-axis. Grouping requires \code{DORCs}
to be specified and disables row clustering (clustering across facets is
not meaningful).

Specific TFs can be visually emphasized via \code{highlight_TFs}, which both
colors/bolds the corresponding x-axis labels and (optionally) draws a
rectangle around the entire column for each highlighted TF. The label and
box colors can be set independently.
}
