% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudotime.R
\name{smooth_feature_matrix}
\alias{smooth_feature_matrix}
\title{Smooth many features into a matrix for heatmap plotting}
\usage{
smooth_feature_matrix(
  seu,
  features,
  time_col,
  assay = "DORC",
  slot = "data",
  window_size = 100,
  loess_alpha = 0.1,
  use_loess = TRUE,
  cells = NULL,
  verbose = TRUE
)
}
\arguments{
\item{seu}{A Seurat object.}

\item{features}{Character vector of features to include (genes, peaks,
or motifs depending on \code{assay}).}

\item{time_col}{Character(1). Pseudotime metadata column.}

\item{assay}{Character(1). Assay to pull from. Default \code{"DORC"}.}

\item{slot}{Character(1). Slot within the assay. Default \code{"data"}.}

\item{window_size}{Integer. Cells per sliding window. Default 100.}

\item{loess_alpha}{Numeric in (0, 1]. LOESS span. Default 0.1.}

\item{use_loess}{Logical. If TRUE (default), apply LOESS smoothing to
each row after the rolling-mean step. Set FALSE on
sparse features (e.g. raw ATAC peaks) where LOESS may
issue numerical warnings.}

\item{cells}{Optional cell barcodes to restrict to. Default NULL
(all cells).}

\item{verbose}{Logical. Print a status message. Default TRUE.}
}
\value{
Named list:
\describe{
\item{matrix}{Numeric matrix, rows = features, cols = pseudotime
windows, values in [0, 1].}
\item{pseudotime}{Numeric vector of smoothed pseudotime values,
one per column of \code{matrix}.}
\item{assay}{Echo of the assay name, used by the plotter for titles.}
}
}
\description{
Applies the same rolling-mean + 1st–99th percentile min–max normalization
as \code{\link{smooth_feature}} to every feature in \code{features}, optionally
followed by a LOESS fit (with values re-clipped to [0, 1]). Returns a
matrix suitable for \code{\link{plot_feature_heatmap}}: rows are features,
columns are sliding-window indices along the pseudotime axis.
}
\details{
Each row is normalized independently, so the heatmap shows the
\emph{shape / timing} of each feature's response rather than its absolute
magnitude. Features requested but not present in the assay are silently
dropped; if none remain, the function errors.
}
\examples{
\dontrun{
sm <- smooth_feature_matrix(seu, head(rownames(seu[["DORC"]]), 80),
                            "stim_pseudotime", assay = "DORC")
plot_feature_heatmap(sm)
}
}
\seealso{
\code{\link{plot_feature_heatmap}}.
}
