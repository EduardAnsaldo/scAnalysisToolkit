% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudotime.R
\name{.rollmean_norm}
\alias{.rollmean_norm}
\title{Rolling mean + 1st–99th percentile min–max normalization (internal)}
\usage{
.rollmean_norm(x, window_size)
}
\arguments{
\item{x}{Numeric vector (cells ordered by pseudotime upstream).}

\item{window_size}{Integer. Number of cells per sliding window.}
}
\value{
Numeric vector of length \code{length(x) - window_size + 1}, values
in \verb{[0, 1]}. Returns all zeros if the feature has zero dynamic range.
}
\description{
Applies a centered rolling mean with window size \code{window_size}, then
rescales the smoothed values so that the 1st percentile maps to 0 and
the 99th percentile maps to 1. Values are clipped to \verb{[0, 1]}.
}
\details{
Robust to NAs in the input (uses \code{na.rm = TRUE} within each window via
\code{zoo::rollapply}) and to flat features (returns all-zero vector when
the 1–99\\% range is zero, instead of \code{NaN}/\code{Inf}).
}
\keyword{internal}
