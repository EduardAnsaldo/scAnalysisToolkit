% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudotime.R
\name{plot_feature_heatmap}
\alias{plot_feature_heatmap}
\title{Heatmap of many smoothed features along pseudotime}
\usage{
plot_feature_heatmap(
  smoothed,
  order_by = c("peak", "hclust", "none"),
  show_row_names = TRUE,
  title = NULL,
  col_low = "#352A87",
  col_mid = "#FAFA6E",
  col_high = "#A50026",
  annotate_time = TRUE,
  base_family = "Helvetica",
  base_size = 10,
  row_names_family = NULL,
  row_names_size = 8,
  row_names_face = "plain",
  col_title_family = NULL,
  col_title_size = 12,
  col_title_face = "bold",
  row_title_family = NULL,
  row_title_size = NULL,
  row_title_face = "plain",
  legend_title_family = NULL,
  legend_title_size = 10,
  legend_title_face = "bold",
  legend_labels_family = NULL,
  legend_labels_size = 9,
  legend_labels_face = "plain",
  anno_name_family = NULL,
  anno_name_size = 9,
  anno_name_face = "plain"
)
}
\arguments{
\item{smoothed}{List from \code{\link{smooth_feature_matrix}}.}

\item{order_by}{One of \code{"peak"}, \code{"hclust"}, or \code{"none"}. Default
\code{"peak"}.}

\item{show_row_names}{Logical. Show feature names on the rows. Default TRUE.
Turn off for >100 features unless plotting very tall.}

\item{title}{Optional column title. NULL uses an auto-generated
title from the assay name.}

\item{col_low, col_mid, col_high}{Colors for the 0 / 0.5 / 1 stops of the
color ramp. Defaults are a viridis-style purple →
yellow → red.}

\item{annotate_time}{Logical. Draw a line plot of pseudotime above the
heatmap to indicate how time is distributed across
columns (useful since cells often pile up at the
endpoints). Default TRUE.}

\item{base_family}{Default \code{fontfamily} for all text. Default "Helvetica".}

\item{base_size}{Default \code{fontsize} for all text. Default 10.}

\item{row_names_family, row_names_size, row_names_face}{Row label font.}

\item{col_title_family, col_title_size, col_title_face}{Column title font.}

\item{row_title_family, row_title_size, row_title_face}{Row title font
(only visible if rows are split into groups).}

\item{legend_title_family, legend_title_size, legend_title_face}{Color-scale legend title font.}

\item{legend_labels_family, legend_labels_size, legend_labels_face}{Color-scale legend tick-label font.}

\item{anno_name_family, anno_name_size, anno_name_face}{Font for the "Pseudotime" label on the top annotation.}
}
\value{
A \code{ComplexHeatmap::Heatmap} object. Print to draw, or compose
with \code{+} and pass to \code{draw()} for legends/annotations control.
}
\description{
Renders a \code{ComplexHeatmap::Heatmap} from the output of
\code{\link{smooth_feature_matrix}}. Rows are features, columns are
sliding-window positions along the pseudotime axis, and cell color
encodes the row-normalized signal (0 = feature's 1st percentile,
1 = its 99th percentile).
}
\details{
Use \code{order_by = "peak"} for the classic "wave" presentation where
rows are sorted by the column index of each feature's maximum —
early-responding features at the top, late-responding at the bottom.
Use \code{"hclust"} to instead group features by similarity of trajectory
shape regardless of timing.

All text elements expose font controls. Anything left at its default
(or NULL for the family arguments) inherits from \code{base_family} and
\code{base_size}. Note: the chosen \code{fontfamily} must be available to the
active graphics device — see \code{extrafont} or \code{cairo_pdf()} for PDFs.

Each \verb{*_family} / \verb{*_size} / \verb{*_face} argument is independent; leave one
at its default to inherit. \code{fontface} accepts \code{"plain"}, \code{"italic"},
\code{"bold"}, or \code{"bold.italic"}.
}
\seealso{
\code{\link{smooth_feature_matrix}}.
}
