% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClusterProfiler_GO_functions.r
\name{GO_overrepresentation_analysis_multiple_lists}
\alias{GO_overrepresentation_analysis_multiple_lists}
\title{GO Overrepresentation Analysis for Multiple Gene Lists}
\usage{
GO_overrepresentation_analysis_multiple_lists(
  gene_list,
  all_genes,
  local_path,
  ontology = "ALL",
  minGSSize = 5,
  maxGSSize = 400,
  filename = "",
  drop_levels = F,
  levels_to_drop = c(),
  simplify_function = min,
  simplify_by = "p.adjust",
  simplify_terms = F,
  run_network = F,
  network_n_terms = 100,
  nterms_to_plot = 5,
  font_size = 8,
  grouping_var = "",
  ...
)
}
\arguments{
\item{gene_list}{Named list; each element contains gene symbols for a group/cluster}

\item{all_genes}{Character vector; background/universe gene symbols}

\item{local_path}{Character; path to save results and plots}

\item{ontology}{Character; GO ontology ('BP', 'MF', 'CC', or 'ALL'). Default 'ALL'}

\item{minGSSize}{Integer; minimum gene set size. Default 5}

\item{maxGSSize}{Integer; maximum gene set size. Default 400}

\item{filename}{Character; prefix for output files. Default ''}

\item{drop_levels}{Logical; whether to drop specific GO levels. Default FALSE}

\item{levels_to_drop}{Integer vector; GO levels to exclude if drop_levels=TRUE. Default c()}

\item{simplify_function}{Function; function for simplifying redundant terms (min, max, mean). Default min}

\item{simplify_by}{Character; metric for simplification ('p.adjust', 'pvalue'). Default 'p.adjust'}

\item{simplify_terms}{Logical; whether to remove redundant GO terms. Default FALSE}

\item{run_network}{Logical; whether to create enrichment network plots. Default FALSE}

\item{network_n_terms}{Integer; number of terms to include in network. Default 100}

\item{nterms_to_plot}{Integer; number of terms to show per cluster in dot plot. Default 5}

\item{font_size}{Numeric; font size for plots. Default 8}

\item{grouping_var}{Character; label for x-axis grouping variable. Default ''}

\item{...}{Additional arguments (unused)}
}
\value{
List with two elements: plot (ggplot object) and results (enrichment results)
}
\description{
Performs Gene Ontology (GO) overrepresentation analysis on multiple gene lists using
clusterProfiler's compareCluster with enrichGO. Compares enriched GO terms across
multiple groups/clusters simultaneously and generates a comparative dot plot visualization.
}
\details{
Uses mouse annotation database (org.Mm.eg.db) with gene symbols. Creates a dot plot
comparing enriched GO terms across all provided gene lists. Automatically orders
clusters numerically if possible.
}
