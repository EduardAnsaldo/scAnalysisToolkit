% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.r
\name{CoveragePlot_custom}
\alias{CoveragePlot_custom}
\title{Custom CoveragePlot with stacked annotation, peak, and link tracks}
\usage{
CoveragePlot_custom(
  seurat,
  region,
  theme = NULL,
  title = NULL,
  subtitle = NULL,
  title_theme = NULL,
  assay_atac = "ATAC",
  extend_upstream = 20000,
  extend_downstream = 20000,
  annotation_font_size = 1.5,
  add_links = FALSE,
  gene_symbol = FALSE,
  expression_feature = NULL,
  assay = "SCT",
  heights = c(10, 1, 2, 3),
  widths = c(10, 1)
)
}
\arguments{
\item{seurat}{A \code{Seurat} object containing a \code{ChromatinAssay} (typically
named \code{"ATAC"}) with fragment information and gene annotations attached.
For \code{gene_symbol = TRUE}, the object must also contain an expression
assay (e.g. \code{"SCT"} or \code{"RNA"}) with the feature of interest.}

\item{region}{Either a coordinate string (e.g. \code{"chr1-1000-2000"} or
\code{"chr1:1000-2000"}) or a gene symbol present in the chromatin assay's
annotations. Gene symbols are resolved to their full transcript span via
\code{\link[=resolve_region]{resolve_region()}}.}

\item{theme}{Optional \code{ggplot2} theme object applied to every track before
the function's internal styling. Use this to set base font sizes, panel
backgrounds, etc.}

\item{title}{Optional plot title shown above the assembled tracks.}

\item{subtitle}{Optional plot subtitle shown beneath the title.}

\item{title_theme}{Optional \code{ggplot2} theme controlling the appearance of
the title and subtitle. If \code{NULL}, a sensible default is used (centered,
bold title at size 16; centered subtitle at size 12).}

\item{assay_atac}{Name of the chromatin assay in \code{seurat}. Defaults to
\code{"ATAC"}. Used both for plotting and for resolving gene symbols against
the assay's annotations.}

\item{extend_upstream}{Integer. Number of bases to extend the plotted
region upstream of \code{region}. Defaults to \code{20000}.}

\item{extend_downstream}{Integer. Number of bases to extend the plotted
region downstream of \code{region}. Defaults to \code{20000}.}

\item{annotation_font_size}{Numeric. Font size of the gene/transcript name
labels on the gene annotation track, passed to \code{AnnotationPlot_custom()}.
Note this is in ggplot2's geom text-size units (mm-based), not points.
Defaults to \code{1.5}.}

\item{add_links}{Logical. If \code{TRUE}, a \code{LinkPlot} track is added showing
peak-to-gene links stored in \code{Links(seurat[[assay_atac]])}. Defaults to
\code{FALSE}.}

\item{gene_symbol}{Logical. If \code{TRUE}, an \code{ExpressionPlot} panel is
appended to the right of the tracks for the feature given by
\code{expression_feature} (or \code{region} itself if \code{expression_feature} is
\code{NULL}). Defaults to \code{FALSE}.}

\item{expression_feature}{Optional character string giving the feature
(typically a gene symbol) to plot in the expression panel. Only used
when \code{gene_symbol = TRUE}. If \code{NULL}, the value of \code{region} is used. A
warning is issued and the panel is dropped if the feature is not found
in \code{assay}.}

\item{assay}{Name of the expression assay used for the expression panel.
Defaults to \code{"SCT"}.}

\item{heights}{Numeric vector of relative heights for the tracks in this
order: coverage, peak, gene annotation, link. Length 4 by default
(\code{c(10, 1, 2, 3)}); entries corresponding to omitted tracks (e.g. the
link track when \code{add_links = FALSE}) are dropped automatically.}

\item{widths}{Numeric vector of length 2 giving the relative widths of
the track panel and the expression panel, passed to \code{\link[Signac:CombineTracks]{CombineTracks()}}.
Defaults to \code{c(10, 1)}.}
}
\value{
A \code{patchwork} object combining the coverage and (optionally)
expression panels. Can be printed directly, further composed with other
\code{patchwork} objects, or saved with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.
}
\description{
Builds a publication-ready Signac coverage plot for a single genomic region,
combining a coverage track with peak, gene annotation, and (optionally)
peak-to-gene link tracks, plus an optional expression panel on the right.
Y-axis ticks and labels are stripped from all tracks for a clean look,
facet strip labels are rotated, and legends are removed. The user can pass
an additional \code{ggplot2} theme that is applied before the function's own
styling adjustments so those adjustments always win.
}
\details{
Tracks are assembled with \code{\link[Signac:CombineTracks]{Signac::CombineTracks()}} in this order from
top to bottom: coverage, peak, gene annotation, link. Any of these can be
omitted (links via \code{add_links = FALSE}; the others are always present).

Styling is applied in three passes so user customizations and internal
adjustments compose predictably:
\enumerate{
\item The user-supplied \code{theme} (if any) is added to every track.
\item Y-axis ticks/labels are blanked, the coverage y-label is set to
\code{"Normalized signal"}, and legends are removed.
\item Facet strip labels are rotated on the coverage plot and removed
from the expression panel.
}

This ordering ensures the function's facet-label rotation and y-axis
stripping cannot be overridden by a user theme that resets \code{strip.text}
or \code{axis.text.y}.
}
\section{Required helpers}{

This function depends on \code{AnnotationPlot_custom()} (a custom variant of
\code{\link[Signac:AnnotationPlot]{Signac::AnnotationPlot()}}) and a \code{sequential_palette_dotplot} palette
object available in the calling environment.
}

\examples{
\dontrun{
# Basic usage with a gene symbol
CoveragePlot_custom(seurat_obj, region = "Lypla1")

# Coordinate region with link track and expression panel
CoveragePlot_custom(
  seurat_obj,
  region            = "chr1-4768107-4769042",
  add_links         = TRUE,
  gene_symbol       = TRUE,
  expression_feature = "Lypla1",
  extend_upstream   = 50000,
  extend_downstream = 50000,
  title             = "Lypla1 locus",
  subtitle          = "scATAC + scRNA"
)

# Apply a custom base theme
CoveragePlot_custom(
  seurat_obj,
  region = "Tcea1",
  theme  = theme_classic(base_size = 11)
)
}

}
\seealso{
\code{\link[Signac:CoveragePlot]{Signac::CoveragePlot()}}, \code{\link[Signac:PeakPlot]{Signac::PeakPlot()}}, \code{\link[Signac:LinkPlot]{Signac::LinkPlot()}},
\code{\link[Signac:ExpressionPlot]{Signac::ExpressionPlot()}}, \code{\link[Signac:CombineTracks]{Signac::CombineTracks()}}
}
