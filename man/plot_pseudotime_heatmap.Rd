% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudotime.R
\name{plot_pseudotime_heatmap}
\alias{plot_pseudotime_heatmap}
\title{Plot a heatmap of cells x genes, ordered by pseudotime}
\usage{
plot_pseudotime_heatmap(
  seurat_obj,
  genes,
  pseudotime_col = "pseudotime",
  assay = NULL,
  slot = "data",
  normalize = c("minmax", "minmax_pct", "zscore", "none"),
  pct_low = 0.01,
  pct_high = 0.99,
  smooth_window = NULL,
  loess_smooth = FALSE,
  loess_span = 0.1,
  gene_order = c("input", "peak", "hclust"),
  cell_subset = NULL,
  show_pseudotime_bar = TRUE,
  palette = NULL,
  palette_direction = 1,
  pseudotime_palette = "magma"
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{genes}{Character vector of genes (features) to plot.}

\item{pseudotime_col}{Name of the column in \code{seurat_obj@meta.data} used to
order cells. Default "pseudotime".}

\item{assay}{Assay to pull expression from. Default DefaultAssay(seurat_obj).}

\item{slot}{Slot to pull expression from ("data", "counts", "scale.data").
Default "data" (log-normalized).}

\item{normalize}{How to normalize each gene across cells before plotting:
"minmax"      -> scale to [0, 1] using absolute min/max
"minmax_pct"  -> scale to [0, 1] using 1st/99th percentile
(clipped); matches the Ma et al. /
SHARE-seq DORC convention.
"zscore"      -> per-gene z-score (clipped at +/-3)
"none"        -> no normalization}

\item{pct_low, pct_high}{Percentiles used when normalize = "minmax_pct".}

\item{smooth_window}{If non-NULL and > 1, apply a centered rolling mean of
this window across pseudotime-ordered cells (per gene).}

\item{loess_smooth}{If TRUE, fit a per-gene loess on the (rolling-averaged
and normalized) values vs. the (rolling-averaged)
pseudotime, and plot the loess fit.}

\item{loess_span}{span / alpha for loess. Default 0.1.}

\item{gene_order}{How to order genes on the y-axis:
"input", "peak", or "hclust".}

\item{cell_subset}{Optional character vector of cell names to restrict to.}

\item{show_pseudotime_bar}{If TRUE, add a thin annotation bar on top showing
pseudotime. The bar has NO colorbar legend, only a
label naming the pseudotime column.}

\item{palette}{Color palette for the heatmap (viridis/Brewer name,
vector of colors, or a ggplot Scale object).}

\item{palette_direction}{+1 or -1.}

\item{pseudotime_palette}{Palette for the pseudotime annotation bar.}
}
\value{
A ggplot object (or a patchwork if show_pseudotime_bar = TRUE).
}
\description{
Plot a heatmap of cells x genes, ordered by pseudotime
}
