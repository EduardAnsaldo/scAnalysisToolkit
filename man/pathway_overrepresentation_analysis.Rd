% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClusterProfiler_MSigDB_functions.r
\name{pathway_overrepresentation_analysis}
\alias{pathway_overrepresentation_analysis}
\title{Pathway Overrepresentation Analysis using MSigDB}
\usage{
pathway_overrepresentation_analysis(
  significant_genes,
  all_genes,
  local_path,
  minGSSize = 5,
  maxGSSize = 400,
  filename = "",
  group = "",
  nterms_to_plot = 12,
  font_size = 10,
  label_length = 60,
  wrap_labels = FALSE,
  ...
)
}
\arguments{
\item{significant_genes}{Character vector; gene symbols to test for enrichment}

\item{all_genes}{Character vector; background/universe gene symbols}

\item{local_path}{Character; path to save results and plots}

\item{minGSSize}{Integer; minimum gene set size. Default 5}

\item{maxGSSize}{Integer; maximum gene set size. Default 400}

\item{filename}{Character; prefix for output files. Default ''}

\item{group}{Character; group identifier for plot titles. Default ''}

\item{nterms_to_plot}{Integer; number of terms to show in dot plot. Default 50}

\item{font_size}{Numeric; font size for plots. Default 8}

\item{label_length}{Integer; max character length for y-axis pathway labels.
Used as the truncation width (default) or, when \code{wrap_labels = TRUE},
the wrapping width. Default 60.}

\item{wrap_labels}{Logical; controls long y-axis pathway labels. In both
modes underscores in the pathway names are shown as spaces
(e.g. \code{"REACTOME_IFN_SIGNALING"} -> \code{"REACTOME IFN SIGNALING"}).
When \code{FALSE} (default) labels are then truncated to \code{label_length};
when \code{TRUE} they are wrapped onto multiple lines at \code{label_length}.
Default \code{FALSE}.}

\item{...}{Additional arguments (unused)}
}
\value{
ggplot object; bar plot of enriched pathways (or no-data plot if insufficient genes/results)
}
\description{
Performs pathway overrepresentation analysis using clusterProfiler's enricher
function with MSigDB pathway collections. Tests which pathways are statistically
enriched in a gene list compared to a background.
}
