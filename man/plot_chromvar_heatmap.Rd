% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmaps.r
\name{plot_chromvar_heatmap}
\alias{plot_chromvar_heatmap}
\title{Plot chromVAR motif activity heatmap by group}
\usage{
plot_chromvar_heatmap(
  motifs_to_plot,
  seurat,
  title,
  color_palette = grDevices::hcl.colors(n = 20, "RdBu", rev = TRUE),
  grouping_var = "Samples",
  motif_labels = FALSE
)
}
\arguments{
\item{motifs_to_plot}{Character vector of motif IDs (matching rownames of
the chromVAR assay) to include in the heatmap, in the order they should
appear on the y-axis. If \code{motif_labels = TRUE}, this vector must be
named, with names being the display labels (e.g., TF names) and values
being the motif IDs:
\code{c(Sox2 = "MA0143.4", "Pou5f1::Sox2" = "MA0142.1")}. Duplicated names
are allowed (e.g., when the same TF binds multiple motifs).}

\item{seurat}{A Seurat object with a \code{chromvar} assay (typically created
via \code{Signac::RunChromVAR()}).}

\item{title}{Character string used as the plot title.}

\item{color_palette}{Character vector of colors for the diverging fill
gradient. Defaults to a reversed RdBu palette with 20 steps. Should be
ordered low → high (negative → positive activity).}

\item{grouping_var}{Character string naming the metadata column in
\code{seurat@meta.data} to group cells by (e.g., \code{"Samples"},
\code{"seurat_clusters"}). Each unique value becomes one column in the heatmap.}

\item{motif_labels}{Logical. If \code{TRUE} (default \code{FALSE}), uses the names of
\code{motifs_to_plot} as y-axis labels instead of the motif IDs. Requires
\code{motifs_to_plot} to be a named vector. Duplicated names are displayed
as-is (the same label can appear multiple times on the y-axis).}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a heatmap visualizing the mean chromVAR motif activity (deviation
z-scores) across groups (e.g., samples, clusters, conditions). Unlike RNA
expression, chromVAR scores are already z-scores relative to a GC-matched
background, so they are averaged per group without further scaling. The
color scale is centered at 0 with symmetric limits, so white represents
baseline activity, red represents above-baseline, and blue represents
below-baseline.
}
\details{
The function:
\enumerate{
\item Pulls the \code{data} slot from the \code{chromvar} assay.
\item Computes the mean activity per group per motif using \code{rowMeans()}.
\item Reshapes to long format and orders motifs/samples as factors.
\item Builds a tile plot with a diverging fill scale centered at 0, with
symmetric limits set by the maximum absolute mean activity.
}

Note: chromVAR scores can be negative, so this function uses \code{rowMeans()}
rather than \code{Seurat::AggregateExpression()}, which is designed for
log-normalized counts and would distort z-scored data.

Internally, when \code{motif_labels = TRUE}, the y-axis uses the motif IDs as
unique factor levels (with display labels supplied via \code{scale_y_discrete}),
so duplicated TF names render correctly without being collapsed into a
single row. Motif IDs in \code{motifs_to_plot} must themselves be unique.
}
\examples{
\dontrun{
# With TF names as labels, including a duplicated TF name
plot_chromvar_heatmap(
  motifs_to_plot = c(Sox2 = "MA0143.4",
                     Sox2 = "MA0143.3",      # same TF, different motif
                     "Pou5f1::Sox2" = "MA0142.1",
                     Klf4 = "MA0039.4"),
  seurat = seurat_obj,
  title = "Pluripotency TFs",
  grouping_var = "seurat_clusters",
  motif_labels = TRUE
)
}

}
