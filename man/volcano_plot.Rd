% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de_visualization.r
\name{volcano_plot}
\alias{volcano_plot}
\title{Create Volcano Plot for Differential Expression Results}
\usage{
volcano_plot(
  results,
  group1,
  group2,
  cluster,
  local_figures_path,
  FC_threshold,
  p_value_threshold,
  max_overlaps = 15,
  label_size = 5,
  my_colors = c("#023FA5", "#8E063B", "gray"),
  test_type = c("Wilcox", "Pseudobulk", "Bulk", "Wilcox_ATAC", "Wilcox_DORC",
    "Wilcox_ATAC_closest_genes", "Wilcox_ChromVar_motif"),
  genes_to_plot = NULL,
  pt_size = 1.5,
  pval_cap = 300,
  y_axis_max = NULL,
  nudge_x = NULL,
  nudge_y = NULL,
  label_line_width = 0.5,
  dashed_line_width = 0.5,
  ...
)
}
\arguments{
\item{results}{Data frame containing DE results with columns: genes, log2FoldChange,
padj, pvalue}

\item{group1}{Character; name of first comparison group}

\item{group2}{Character; name of second comparison group}

\item{cluster}{Character; cluster or cell type identifier}

\item{local_figures_path}{Character; directory path to save the plot}

\item{FC_threshold}{Numeric; log2 fold change threshold for significance}

\item{p_value_threshold}{Numeric; adjusted p-value threshold for significance}

\item{max_overlaps}{Integer; maximum label overlaps allowed. Default 15}

\item{label_size}{Numeric; size of gene labels. Default 5}

\item{my_colors}{Character vector of length 3; colors for DOWN, UP, NO.}

\item{test_type}{Character; type of test. Options: 'Wilcox', 'Pseudobulk', 'Bulk',
'Wilcox_ATAC', 'Wilcox_ATAC_closest_genes', 'Wilcox_DORC', 'Wilcox_ChromVar_motif'. Default 'Wilcox'}

\item{genes_to_plot}{Character vector; specific genes to highlight. Default NULL}

\item{pt_size}{Numeric; point size. Default 1.5}

\item{pval_cap}{Numeric; cap applied to the data's -log10 adjusted p-values to
avoid infinities (padj is floored at 10^-pval_cap). This clips the plotted
point values, not the displayed axis range. Must stay below ~307 so the
floor 10^-pval_cap does not underflow to 0 in double precision. Default 300}

\item{y_axis_max}{Numeric or NULL; upper limit of the y-axis (-log10 adjusted
p-value). If NULL, the axis is scaled to the data maximum. Useful for keeping
a consistent y-axis range across multiple plots. Default NULL}

\item{nudge_x}{Numeric or NULL; horizontal nudge for gene labels. If NULL,
defaults to 0 (or 3 when genes_to_plot is provided). Default NULL}

\item{nudge_y}{Numeric or NULL; vertical nudge for gene labels. If NULL,
defaults to 0 (or 3 when genes_to_plot is provided). Default NULL}

\item{label_line_width}{Numeric; line width of the leader segments connecting
gene labels to their points (the geom_text_repel \code{segment.size}). Default 0.5}

\item{dashed_line_width}{Numeric; line width of the dashed fold-change and
p-value threshold guide lines. Default 0.5}

\item{...}{Additional arguments passed to plotting functions}
}
\value{
A ggplot object
}
\description{
Generates a volcano plot displaying log2 fold change vs. -log10 adjusted p-value,
with genes colored by differential expression status. Supports both RNA-seq and
ATAC-seq data. Automatically removes extreme non-significant genes to improve
visualization.
}
