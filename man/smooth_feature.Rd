% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudotime.R
\name{smooth_feature}
\alias{smooth_feature}
\title{Smooth one feature along pseudotime and fit a LOESS curve}
\usage{
smooth_feature(
  seu,
  feature,
  time_col,
  assay = "DORC",
  slot = "data",
  window_size = 100,
  loess_alpha = 0.1,
  cells = NULL
)
}
\arguments{
\item{seu}{A Seurat object. Must contain \code{assay} and \code{time_col}.}

\item{feature}{Character(1). Feature to extract — must be in
\code{rownames(GetAssayData(seu, assay, slot))}.}

\item{time_col}{Character(1). Name of the metadata column holding the
pseudotime / NN stimulation time values.}

\item{assay}{Character(1). Assay to pull from. Default \code{"DORC"}.
Other typical choices: \code{"RNA"}, \code{"ATAC"}, \code{"chromvar"}.}

\item{slot}{Character(1). Slot within the assay. Default \code{"data"}.}

\item{window_size}{Integer. Cells per sliding window. Default 100.}

\item{loess_alpha}{Numeric in (0, 1]. LOESS span. Default 0.1.}

\item{cells}{Optional character vector of cell barcodes to use.
NULL (default) uses all cells in \code{seu}.}
}
\value{
A named list:
\describe{
\item{feature}{The feature name (echoed back).}
\item{assay}{The assay name (echoed back).}
\item{data}{A data.frame with columns
\code{pseudotime} (smoothed time axis),
\code{value} (smoothed + 1–99\\% normalized feature signal), and
\code{fit} (LOESS predictions on the smoothed grid).}
\item{loess}{The fitted \code{loess} object, for downstream prediction
or diagnostics.}
}
}
\description{
For a single feature (gene, peak, or motif) in a chosen Seurat assay:
orders cells by pseudotime, computes a centered rolling mean over
\code{window_size} cells, min-max normalizes the result to its 1st–99th
percentile range, and fits a LOESS smoother with span \code{loess_alpha}
against the (also rolling-mean smoothed) pseudotime axis.
}
\details{
This is the per-feature engine behind \code{\link{plot_feature}}.
}
\examples{
\dontrun{
res <- smooth_feature(seu, "IRF4", "stim_pseudotime", assay = "DORC")
plot_feature(res)
}
}
\seealso{
\code{\link{plot_feature}}, \code{\link{smooth_feature_matrix}}.
}
